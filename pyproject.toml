[project]
name = "sonus-auris-test-chaos-recovery-tests"
version = "0.1.0"
description = "Deterministic fault-injection, crash recovery, retry, partition, duplicate-delivery, and convergence tests."
requires-python = ">=3.11"
dependencies = []

[tool.deep-tests]
suite = "chaos"
deterministic = true
network_default = "disabled"
